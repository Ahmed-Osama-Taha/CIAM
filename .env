# SSO Configuration for Kubernetes Deployment
VITE_SSO_REDIRECT_URI=http://172.20.10.3:30007
VITE_SSO_AUTH_SERVER_URL=http://172.20.10.5:31830
VITE_SSO_REALM=ahmed
VITE_SSO_CLIENT_ID=pathfinder

# Backend API Configuration
# Option 1: If you have a separate backend API running in K8s
# VITE_SERVER_URI=http://YOUR_BACKEND_SERVICE_IP:PORT

# Option 2: If you have a separate backend API running externally
# VITE_SERVER_URI=http://YOUR_BACKEND_IP:PORT

# Option 3: If testing without a backend (will show error message)
VITE_SERVER_URI=http://localhost:8080

# Option 4: If you have a backend on a different NodePort (example)
# VITE_SERVER_URI=http://172.20.10.3:30008